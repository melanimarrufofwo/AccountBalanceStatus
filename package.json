{
  "name": "tesra",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tesra",
    "create-local": "graph create --node http://localhost:8020/ tesra",
    "remove-local": "graph remove --node http://localhost:8020/ tesra",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tesra"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
